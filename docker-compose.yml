services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: itinera
      POSTGRES_PASSWORD: itinera
      POSTGRES_DB: itinera
    ports:
      - "5432:5432"
    volumes:
      - itinera_pgdata:/var/lib/postgresql/data

volumes:
  itinera_pgdata:
